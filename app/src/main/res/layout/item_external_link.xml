<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal">

    <ImageView
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/url_title"
        app:layout_constraintBottom_toBottomOf="parent"
        android:id="@+id/url_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/whoicon"
        android:padding="8dp"
        android:contentDescription="@string/profile_image" />

    <TextView
        app:layout_constraintStart_toEndOf="@id/url_image"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/web_icon"
        app:layout_constraintBottom_toBottomOf="parent"
        android:id="@+id/url_title"
        android:text="URL ThhhhhhhhhhhhhhhhhhhhhhhhhhhhhILE"
        style="@style/TextAppearance.AppCompat.Medium"
        android:textColor="@color/colorPrimary"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_gravity="center_horizontal|center_vertical"/>

    <ImageView
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:id="@+id/web_icon"
        android:layout_width="@dimen/link_icon_height"
        android:layout_height="@dimen/link_icon_height"
        android:padding="8dp"
        android:src="@drawable/webicon"/>

</androidx.constraintlayout.widget.ConstraintLayout>